name: CI

on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

# Prefer Node 24 for JS actions that still declare node20 (GitHub deprecation path).
env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  quality:
    name: format · lint · typecheck · test
    runs-on: ubuntu-latest
    timeout-minutes: 20

    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Setup pnpm
        uses: pnpm/action-setup@v6

      - name: Setup Node.js
        uses: actions/setup-node@v7
        with:
          node-version: '24.18.0'
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Format check
        run: pnpm format

      - name: Lint
        run: pnpm lint

      - name: Typecheck
        run: pnpm typecheck

      - name: Unit + integration tests
        run: pnpm test

      - name: Coverage (informational)
        run: pnpm test:coverage
        continue-on-error: true

      - name: Upload coverage lcov
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: coverage-lcov
          path: coverage/lcov.info
          if-no-files-found: ignore

  build:
    name: build API + dashboard + docs
    runs-on: ubuntu-latest
    timeout-minutes: 25
    needs: quality

    steps:
      - uses: actions/checkout@v7

      - uses: pnpm/action-setup@v6

      - uses: actions/setup-node@v7
        with:
          node-version: '24.18.0'
          cache: pnpm

      - name: Install root deps
        run: pnpm install --frozen-lockfile

      - name: Install dashboard deps
        run: pnpm --dir dashboard install --frozen-lockfile

      - name: Install docs-site deps
        run: |
          if [ -f docs-site/pnpm-lock.yaml ]; then
            pnpm --dir docs-site install --frozen-lockfile
          else
            npm --prefix docs-site ci || npm --prefix docs-site install
          fi

      - name: Build all
        run: pnpm build

  docker:
    name: docker build
    runs-on: ubuntu-latest
    timeout-minutes: 40
    needs: quality

    steps:
      - uses: actions/checkout@v7

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4

      - name: Build image (no push)
        uses: docker/build-push-action@v7
        with:
          context: .
          push: false
          tags: zapo-rest:ci
          provenance: false
          sbom: false
          cache-from: type=gha
          cache-to: type=gha,mode=max
